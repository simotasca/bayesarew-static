---
import IndexLayout from "../layouts/IndexLayout.astro";
import { canonical } from "../lib/language";

const t = (await Astro.glob("../content/it/index.md"))[0].frontmatter;
---

<IndexLayout t={t} language={canonical} baseUrl="/" />
